function T_out = conduct(T, dr, dt, k, A, rho_cp_dV, Q_cmb)

qe = -k*(T(2:end)-T(1:end-1))./dr; % W
Qe = zeros(1, length(
Qe = qe.*A(2:end-1); % W
Q = zeros(1,length(T));
Q(2:end-1) = Qe(2:end)-Qe(1:end-1); % W
dT = -Q.*dt ./rho_cp_dV;
T_out = T+dT;
end